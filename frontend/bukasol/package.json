{
    "private": true,
    "type": "module",
    "scripts": {
        "clear": "cls && php artisan config:clear && php artisan cache:clear && php artisan route:clear",
        "build": "npm run clear",
        "start": "concurrently \"php artisan schedule:work\" \"php artisan serve --host=0.0.0.0\"",
        "dev": "vite"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.20",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.0",
        "postcss": "^8.4.47",
        "tailwindcss": "^3.4.13",
        "vite": "^5.0"
    }
}